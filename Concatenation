function nonMutatingConcat(original, attach) {
  // original array is called and attach array is used as an argument for the concat method

let newArr = original.concat(attach)
return newArr
}

const first = [1, 2, 3];
const second = [4, 5];
nonMutatingConcat(first, second);
